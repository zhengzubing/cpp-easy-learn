本质：

std::string 是一个动态管理字符数组的模板类。
其内部核心有三个成员变量：
- 指向字符数据的指针（如 _M_data 或 _M_dataplus）
- 当前字符串的长度（如 _M_length）
- 当前分配的缓冲区容量（如 _M_capacity）

现代实现（如 GCC libstdc++、Clang libc++、MSVC STL）通常采用**短字符串优化（SSO）**，即对象内部直接存放一小段字符缓冲区。

一个空的 std::string，即使没有实际字符串内容，仍然会占有：
- SSO 缓冲区（通常 15~23 字节）
- 长度、容量等成员变量