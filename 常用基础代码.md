# 使用vector代替new

```c++
std::vector<uint8_t> vec;
vector.resize(width*height);
int* ptr = vec.data(); // 传给需要 int* 的 C 函数

// vector传递给string
std::vector<char> vec = {'A', 'B', 'C', '\0', 'D'}; // 中间有 '\0'
std::string str(vec.data(), vec.size()); // 包含 '\0' 后的内容

std::vector<uint8_t> vec;
vector.resize(width*height);
std::string str(reinterpret_cast<const char*>(vec.data()), vec.size());

// ✅ 把 string 转回 vector<uint8_t>
std::vector<uint8_t> vec2(str.begin(), str.end());
```


