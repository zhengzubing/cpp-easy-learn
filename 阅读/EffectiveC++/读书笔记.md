# 条款 1 视C++为一个语言联邦
c++ == c + (c with class) + template + STL
C++ 高效编程守则视状况而变化, 取决于你使用 C++的哪一部分

# 条款 2 替换 #define
常量使用 const/constexpr/enum替换
宏函数使用 inline函数替换
考虑constexpr函数

# 条款 3 
使用 const帮助编译器检测错误用法, 比如 if (a*b = c)
使用概念上的常量, 不一定是编译器常量
```c++
class CTexBlock {
    mutable std::size_t textLength;
    mutable bool lengthIsValid;
}

std::size_t CTexBlock::length() const {
    if (!lengthIsValid) {
        textLength = std::strlen(pText);
        lengthIsValid = true;
    }
    return textLength;
}
```
可用 no-const版本调用 const 版本
```c++
char& operator[](std::size_t position) {
    return const_cast<cahr&>(static_cast<const TextBlock&>(*this)[position]);
}
```

# 条款 4 确定对象被使用前已先被初始化
1. 为内置对象进行手动初始化, 因为 C++不保证初始化它们;
2. 使用初始化列表而不是构造函数体中赋值;
3. 为免除跨编译单元的初始化次序问题(非局部静态对象（non-local static） 的初始化顺序在不同编译单元（.cpp 文件）之间是未定义的), 使用 local static对象替换 non-local static对象

```c++
// 有问题
class Month {
private:
    int m_value;
    // 私有构造函数，防止外部创建实例
    Month(int value) : m_value(value) {}

public:
    // 静态成员变量声明
    static const Month Jan;
    static const Month Feb;
    static const Month Mar;
    // ... 其他月份
    
    // 可以添加获取值的方法
    int getValue() const { return m_value; }
};
```

```c++
// 没问题
class Month {
private:
    int m_value;
    // 私有构造函数，防止外部创建实例
    Month(int value) : m_value(value) {}

public:
    // 静态成员函数返回局部静态对象
    static const Month& Jan() {
        static const Month instance(1);
        return instance;
    }
    
    static const Month& Feb() {
        static const Month instance(2);
        return instance;
    }
    
    // 其他月份...
    static const Month& Dec() {
        static const Month instance(12);
        return instance;
    }
    
    // 获取月份值的方法
    int value() const { return m_value; }
};  
```

```c++
// 可以尝试
// month.h
class Month {
private:
    int m_value;
    constexpr Month(int value) : m_value(value) {} //  constexpr构造函数

public:
    // 编译期初始化，无跨单元顺序问题
    static constexpr Month Jan{1};
    static constexpr Month Feb{2};
    // ...其他月份
};
```

# 条款 5 了解C++默默编写并调用哪些函数


# 条款 6 若不想使用编译器自动生成的函数, 就该明确拒绝


# 条款 7 为多态基类声明 virtual 析构函数


# 条款 8 别让异常逃离析构函数
1. 如果程序遭遇一个于析构期间发生的异常后无法继续执行, 禁止异常传播或者abort()掉是合理的;
2. 如果需要client对 server 的close 函数运行期间抛出的异常做出反应, 那么clse函数就不应该在 server 的析构函数中调用, 而是提供 close 供 client 调用.

# 条款 9 绝不在构造和析构过程中调用 virtual函数
确保构造函数和析构函数都没有调用 virtual函数, 而且他们调用的所有函数也都服从这一约束;
即使调用了 virtual函数, 这类调用也不会下降至 derived class;

# 条款 10 令 operator=返回一个 reference to *this
为什么不返回 void?
因为要支持 x = y = 10; 这种语法, 即 x.operator=(y.operator=(10)); 因此y.operator=(10)不能为 void;

为什么不返回 const?
因为要满足++(a = b); 这种语法;

# 条款 11 在 operator=中处理自我赋值
```c++
String& operator=(const String& other) {
    // 1. 自我赋值检查：如果地址地址相同，直接返回
    if (this == &other) {
        return *this;
    }

    // 2. 先释放当前对象的资源有资源
    delete[] data;

    // 3. 分配新内存间并复制数据
    length = other.length;
    data = new char[length + 1];
    std::strcpy(data, other.data);

    // 4. 返回当前对象引用
    return *this;
}
```
或者 更现代的做法, 天然处理自我赋值
```c++
String& operator=(String other) {  // 按值传递，自动创建副本
    std::swap(data, other.data);   // 交换当前对象与副本的资源
    std::swap(length, other.length);
    return *this;
}  
```

# 条款 12 复制对象时勿忘其每一个成分
1. copying函数中, 请确保复制所有 local 成员变量, 调用所有 base classes内的适当的 copying函数.
```c++
    // 派生类拷贝构造函数：必须在初始化列表中调用基类拷贝构造
    Derived(const Derived& other) 
        : Base(other)  // 显式调用基类拷贝构造，传入other（会被隐式转换为Base&）
        , age(other.age) {
        std::cout << "Derived 拷贝构造" << std::endl;
    }
```

```c++
    Derived& operator=(const Derived& other) {
        if (this != &other) {  // 检查自我赋值
            // 1. 先调用基类的拷贝赋值运算符，处理基类部分
            Base::operator=(other);  // 显式调用基类的operator=
            
            // 2. 处理派生类自己的成员
            age = other.age;
        }
        std::cout << "Derived 拷贝赋值" << std::endl;
        return *this;
    }
```

3. 不要尝试在某个 copying函数中调用另一个 copying函数以避免代码重复, 应该将共同机能放进第三个函数中, 并由两个 copying函数共同调用.

# 条款 13 以对象管理资源
    在对象析构中释放资源, 以避免忘记释放
    shared_prt管理动态创建的数组时:
```c++
    // 方式1：显式指定删除器为 delete[]
    std::shared_ptr<MyClass> arr1(new MyClass[3]{1, 2, 3}, 
                                 [](MyClass* p) { delete[] p; });

    // 方式2：使用 std::default_delete（C++11 起支持数组特化）
    std::shared_ptr<MyClass> arr2(new MyClass[2]{4, 5}, 
                                 std::default_delete<MyClass[]>());
```

# 条款 14 在资源管理类中小心 copying行为
当一个RAII对象被复制, 会发生什么?
    1. 禁止复制(unique_ptr)
    2. 对底层资源引用计数(shared_ptr)
    3. 复制底层资源(std::string)
    4. 资源转移(unique_ptr)

# 条款 15 在资源管理类中提供对原始资源的访问


# 条款 16 成对使用 new 和 delete 时要采取相同的形式
```c++
typedef float Coordinate[3];  // 语义明确：表示三维坐标（x,y,z）
// 使用时，一眼就能理解变量含义
Coordinate point1 = {1.0f, 2.0f, 3.0f};  // 比 float point1[3] 更清晰
```

# 条款 17 以独立语句将 newd对象置入智能指针
在 C++ 标准中，明确规定了函数实参的求值顺序是未指定的
避免指令重排后又发生了异常, 导致资源泄露


# 条款 18


# 条款 19


# 条款 20



# 条款 21



# 条款 22



# 条款 23



# 条款 24



# 条款 25



# 条款 26



# 条款 27



# 条款 28



# 条款 29



# 条款 30



# 条款 31



# 条款 32



# 条款 33








